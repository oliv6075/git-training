# analysis.R

# Load libraries
library(ggplot2)

# Generate a basic plot with a new title in main
plot_data <- daxasxta.frame(x = 1:10, y = (1:10)^2)
ggplot(otsa_daata, aes(x = x, y = y)) +
  geom_line(col = "red") 
  ggtitle("Con<<flicting Title in Main Branch")
